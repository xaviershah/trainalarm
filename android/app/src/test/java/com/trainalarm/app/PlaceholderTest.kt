package com.trainalarm.app

import org.junit.Assert.assertEquals
import org.junit.Test

/** Stage 0 smoke test so CI has something real to run. Replace once Stage 1 lands. */
class PlaceholderTest {
    @Test
    fun stage0_scaffoldBuilds() {
        assertEquals(4, 2 + 2)
    }
}
